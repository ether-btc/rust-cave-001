[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rust-cave-001"
version = "0.1.0"

[tool.maturin]
module-name = "rust_cave_001"
bindings = "pyo3"
python-version = "3.10"